# ============================================
# 生产环境配置文件
# ============================================
# 
# 使用方法：
# 1. 在服务器项目目录创建 .env.production 文件
# 2. 复制此文件内容并填入真实配置
# 3. 运行 npm run build 构建项目
# 4. 运行 npm run start 或使用PM2启动
#
# ⚠️ 重要：生产环境必须使用强随机密钥！
# ============================================

# 数据库配置（服务器MySQL）
# 注意：使用服务器内网IP，不要用localhost
DATABASE_URL="mysql://数据库用户名:数据库密码@127.0.0.1:3306/数据库名"

# JWT密钥（生产环境必须使用强随机密钥！）
# 生成方法：
#   Windows: -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 64 | % {[char]$_})
#   Linux/Mac: openssl rand -base64 64
# ⚠️ 请务必替换下面的密钥！不要使用默认值！
JWT_SECRET="请在这里粘贴生成的超长随机密钥-至少64位-千万不要用默认值"

# 腾讯云COS配置（生产环境）
COS_SECRET_ID="你的生产环境SecretId"
COS_SECRET_KEY="你的生产环境SecretKey"
COS_BUCKET="你的生产环境存储桶-1234567890"
COS_REGION="ap-beijing"

# 环境标识（生产环境）
NODE_ENV="production"

# 管理员默认密码
# 首次部署后，建议立即登录后台修改密码
ADMIN_DEFAULT_PASSWORD="Admin@123456"

# 生产服务器端口（可选，默认3000）
PORT=3000

# 网站URL（改成你的真实域名！）
# ⚠️ 如果使用HTTPS，必须是 https:// 开头
# 例如：https://www.zetu.com
NEXT_PUBLIC_SITE_URL="https://你的域名.com"

# ============================================
# 可选配置（如需要可取消注释）
# ============================================

# 是否启用Redis缓存（可选）
# REDIS_URL="redis://localhost:6379"

# 邮件服务配置（可选，用于发送通知）
# SMTP_HOST="smtp.qq.com"
# SMTP_PORT="465"
# SMTP_USER="your-email@qq.com"
# SMTP_PASS="your-smtp-password"

# Sentry错误监控（可选）
# SENTRY_DSN="https://xxx@sentry.io/xxx"

